---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: backupdestinations.horde.host
spec:
  group: horde.host
  names:
    kind: BackupDestination
    listKind: BackupDestinationList
    plural: backupdestinations
    singular: backupdestination
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: BackupDestination is the Schema for the backupdestinations API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupDestinationSpec defines the desired state of BackupDestination.
            properties:
              dropbox:
                description: Dropbox contains configuration for backing up to Dropbox
                properties:
                  refreshToken:
                    description: RefreshToken is the OAuth refresh token for Dropbox
                      access
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  remotePath:
                    description: |-
                      RemotePath is the path in Dropbox where backups will be stored
                      If not specified, defaults to /<namespace>/zomboid/<server-name>
                    type: string
                required:
                - refreshToken
                type: object
              s3:
                properties:
                  accessKeyId:
                    description: AccessKeyID is the AWS access key ID for authentication
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  bucketName:
                    description: BucketName is the name of the remote bucket that
                      should be used for storing backups
                    type: string
                  endpoint:
                    description: Endpoint is the FQDN of the S3 storage server. Defaults
                      to s3.amazonaws.com if not specified.
                    type: string
                  endpointCACert:
                    description: EndpointCACert is a PEM encoded CA certificate for
                      validating self-signed certificates
                    type: string
                  endpointInsecure:
                    description: EndpointInsecure disables SSL certificate verification
                      when true
                    type: boolean
                  endpointProtocol:
                    description: EndpointProtocol is the protocol (http/https) to
                      use. Defaults to https.
                    type: string
                  iamRoleEndpoint:
                    description: IAMRoleEndpoint is the endpoint for IAM role credentials
                      (e.g. http://169.254.169.254)
                    type: string
                  partSize:
                    description: PartSize changes the S3 multipart upload part size
                      in MB. Defaults to 16.
                    format: int32
                    minimum: 1
                    type: integer
                  path:
                    description: Path is the path within the bucket where backups
                      will be stored. Must not contain a leading slash.
                    type: string
                  secretAccessKey:
                    description: SecretAccessKey is the AWS secret access key for
                      authentication
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  storageClass:
                    description: StorageClass changes the S3 storage class header.
                      Defaults to STANDARD.
                    type: string
                required:
                - bucketName
                type: object
            type: object
          status:
            description: BackupDestinationStatus defines the observed state of BackupDestination.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
